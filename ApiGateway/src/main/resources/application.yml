server:
  port: 8084

spring:
  application:
    name: APIGATEWAY
  cloud:
    gateway:
      routes:
        - id: USERSERVICE
          uri: lb://USERSERVICE
          predicates:
            - Path=/users/**
            
        - id: HOTELSERVICE
          uri: lb://HOTELSERVICE
          predicates:
            - Path=/hotels/**,/staffs/**
            
        - id: RATINGSERVICE
          uri: lb://RATINGSERVICE
          predicates:
            - Path=/ratings/**

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

# ✅ Okta configuration for API Gateway
#okta:
#  oauth2:
#    issuer: https://trial-6590616.okta.com/oauth2/default
#    audience: api://default 
#    client-id: 0oawwuf8ui7n9yWBP697
#    client-secret: pmbPXH7cKd8OhBPfOBrmyaxjPm78ZA1w2BNh7hxNiv_yQL2yvpTEk-dRVBI6JXJ2
#    scope: openid profile email offline_access
